# Appasamy Frontend Environment Variables
# ----------------------------------------
# Backend API base URL — change if running on a different host/port
REACT_APP_API_URL=http://localhost:8000/api/v1
